import { test, expect, mount, describe, beforeAll } from 'vitest'
import { usePost } from './usePost.ts'

describe('getPostList', () => {
  let wrapper
  beforeAll(() => {
    // wrapper = mount({
    //   template: '<div></div>',
    //   setup() {
    //     return usePost()
    //   }
    // })
  })

  test('getPostList sets postListDetails correctly', async () => {
    // wrapper.vm.response.status = 200
    // await wrapper.vm.getPostList()

    // expect(wrapper.vm.postListDetails).toBeTruthy()
  })
})
